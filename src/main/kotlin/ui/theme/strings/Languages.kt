package ui.theme.strings

enum class Languages(
    val label: String,
    val code: String
) {
    English(
        label = "English",
        code = "en"
    ),
    Hebrew(
        label = "Hebrew",
        code = "he"
    )
}